# Payment Processing Service

Сервис обработки платежей с учётом комиссии, фоновой обработкой и интеграцией с внешним платёжным шлюзом (имитация).

Проект реализуется как тестовое задание и спроектирован с упором на:

* чистую архитектуру
* масштабируемость
* надёжность
* разделение ответственности между слоями

---

## 🧠 Общая идея

Сервис принимает платёж пользователя, гарантированно обрабатывает его в фоне, учитывает комиссию и обновляет баланс пользователя.
API остаётся отзывчивым даже при высокой нагрузке, тяжёлые операции вынесены в отдельные воркеры.

Кроме того, теперь сервис поддерживает создание и хранение пользователей с базовой бизнес-логикой.

---

## 🏗 Архитектура

Проект построен по **слоистой архитектуре (Clean Architecture)**:

```
API → Application → Domain
↓
Infrastructure
```

### Слои:

* **Domain** — бизнес-сущности и правила (не зависит от фреймворков)
* **Application** — use cases и сценарии обработки
* **Infrastructure** — БД, очереди, внешние сервисы
* **API** — HTTP-интерфейс (FastAPI)
* **Workers** — фоновая обработка платежей

---

## 📁 Структура проекта

```
app/
├── api/
│   └── v1/
│       ├── __init__.py        # агрегатор всех роутеров v1
│       ├── payments.py
│       ├── users.py            # добавлен эндпоинт для создания пользователя
│       ├── health.py
│       └── schemas/
│           └── users.py        # схемы для User
├── application/
│   ├── use_cases/
│   │   └── create_user.py      # use case для создания пользователя
│   ├── services/
│   ├── dto/
│   └── interfaces/
├── domain/
│   ├── entities/
│   │   └── user.py             # сущность User
│   ├── enums/
│   └── exceptions.py
├── infrastructure/
│   ├── db/
│   ├── repositories/
│   │   └── user.py              # репозиторий для User
│   ├── payment_gateway/
│   ├── queue/
│   └── cache/
├── workers/
├── core/
│   ├── settings.py
│   ├── logging.py
│   └── dependencies.py
└── main.py
```

---

## 🔌 API versioning

Все HTTP-эндпоинты находятся под версией:

```
/api/v1/*
```

Версия агрегируется в `api/v1/__init__.py`, что позволяет:

* подключать API одной строкой
* легко добавлять новые роутеры
* в будущем безболезненно добавить `v2`

---

## ⚙️ Конфигурация

Конфигурация приложения осуществляется через **environment variables**
и загружается с помощью `pydantic-settings`.

Пример `.env`:

```env
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/payments
REDIS_URL=redis://localhost:6379/0
PAYMENT_GATEWAY_URL=http://localhost:8001
```

---

## 🚀 Запуск (на текущем этапе)

### Установка зависимостей

```bash
poetry install
```

### Запуск приложения

```bash
uvicorn app.main:app --reload
```

### Проверка

* Swagger UI: `http://localhost:8000/docs`
* Health-check: `GET /api/v1/health`
* Создание пользователя: `POST /api/v1/users/`

---

## 📌 Текущий статус проекта

✅ Реализовано:

* базовая структура проекта
* слоистая архитектура
* versioned API
* health-check
* конфигурация через env
* **Создание пользователя**:

  * use case `create_user`
  * схемы пользователя в `schemas/users.py`
  * сущность `User` в `domain/entities/user.py`
  * репозиторий `UserRepository` в `infrastructure/repositories`
  * POST-эндпоинт `/api/v1/users/` для создания пользователя

⏳ В процессе:

* PostgreSQL + SQLAlchemy
* репозитории платежей
* обработка платежей
* фоновые воркеры
* retry + DLQ
* Docker Compose

---

## 🧪 Тестирование

Тесты будут добавляться по мере реализации бизнес-логики
(юнит-тесты для domain и application слоёв).

---

## 📄 Примечание

Проект разрабатывается итеративно.
README будет обновляться по мере добавления функциональности.


